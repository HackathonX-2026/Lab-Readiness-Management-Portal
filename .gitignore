node_modules
dist
dist-ssr
*.local
.vite
.DS_Store
.env.local
.env

tsconfig.tsbuildinfo
*.tsbuildinfo

# Defensive: sync-server DB accidentally created in workspace root if the
# server is started from the wrong cwd. Real DB lives in server/data/.
/data/
